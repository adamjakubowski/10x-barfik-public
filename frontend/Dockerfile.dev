# Development Dockerfile - z hot reload
FROM node:24.12.0-alpine

WORKDIR /app

# Kopiowanie package files
COPY src/package*.json ./

# Instalacja zależności (w tym devDependencies)
RUN npm install

# Kopiowanie kodu (w docker-compose.dev.yml zostanie nadpisane volume)
COPY src/ .

EXPOSE 5173

# Komenda zostanie nadpisana w docker-compose.dev.yml
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
